# @aohys/environment

Future implementation package for the Environment Contract.

The Interface should stay small: validate the current environment, expose safe public values, and fail fast when required configuration is missing or unsafe for the target environment.
