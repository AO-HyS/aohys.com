# @aohys/dashboard-ui

Future implementation package for the Dashboard UI Kit.

This package will hide shadcn/ui primitives behind AOHYS-specific Dashboard Surfaces. The low-level primitive adapter should remain replaceable without rewriting dashboard routes.
