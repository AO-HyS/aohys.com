# @aohys/backend

Future Convex backend surface.

This surface will own backend code for leads, content metadata, media metadata, site settings, resume content, auth integration, and dashboard workflows. Provider configuration should cross the Environment Contract seam instead of reading ad hoc variables.
