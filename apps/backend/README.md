# @aohys/backend

Convex backend surface for AOHYS leads, content, media, site settings, resume versions, auth integration, and dashboard workflows.

Provider configuration should cross the Environment Contract seam instead of reading ad hoc variables.

## Current Foundation

- Convex project: `aohys-com`
- Convex team: `alejandro-ortiz-corro`
- Local/dev deployment created for this repo: `dev/aohys-local`
- Dev deployment URL generated by Convex: `https://patient-bird-955.convex.cloud`
- Dashboard: `https://dashboard.convex.dev/t/alejandro-ortiz-corro/aohys-com`

The local deployment is for development and codegen only. Production deploys should go through GitHub Environments and the Release Train.

## Commands

```sh
pnpm --filter @aohys/backend exec convex dev --once --typecheck=disable --tail-logs disable
pnpm --filter @aohys/backend codegen
pnpm --filter @aohys/backend test
pnpm --filter @aohys/backend typecheck
pnpm --filter @aohys/backend build
```

`convex dev --once` syncs the selected development deployment once without starting a watcher. `codegen` regenerates `convex/_generated`. Convex generated files are committed because backend functions do not typecheck without them.

## Environment Contract

Local development may use `apps/backend/.env.local`, generated by `convex deployment create --select` or `convex dev`. Do not commit real values.

Preview and production values belong in GitHub Environments:

| Variable | Class | Notes |
| --- | --- | --- |
| `CONVEX_URL` | Provider output | Deployment client URL. |
| `CONVEX_SITE_URL` | Provider output | HTTP actions URL. |
| `CONVEX_DEPLOYMENT` | Provider output | Deployment selector. |
| `CONVEX_DEPLOY_KEY` | Server secret | CI/release deploy key. Never expose to browser bundles. |
| `PUBLIC_CONTACT_ENDPOINT` | Public build value | Browser-safe endpoint for the public contact form, usually `${CONVEX_SITE_URL}/contact`. |
| `PUBLIC_POSTHOG_KEY` | Public build value | Used by the contact workflow only for safe conversion metadata. |
| `RESEND_API_KEY` | Server secret | Used by the contact workflow for notification delivery. |

Manual Convex dashboard changes are acceptable during setup or recovery, but durable deploy-time values must be copied back to the matching GitHub Environment.

## Data Model

The first schema includes:

- `leads`
- `mediaMetadata`
- `siteSettings`
- `caseStudyMetadata`
- `resumeVersions`

The public `leads.submit` mutation validates through `src/lead-intake.ts` before inserting into Convex. Reusable normalization and assertion primitives come from `@aohys/core` instead of staying embedded in feature code. Tests exercise this boundary without direct database coupling.

The public contact endpoint is implemented as a Convex HTTP action at `/contact`. It validates the submission, persists the lead through an internal mutation, sends a Resend notification through an injected provider adapter, and captures a PostHog conversion event with metadata only.
