# Domain Docs

AOHYS uses a single domain context.

Before exploring or changing the repository:

- Read `CONTEXT.md`.
- Read relevant ADRs in `docs/adr/`.
- Use the vocabulary defined in `CONTEXT.md` exactly.
- Flag any proposal that contradicts an existing ADR.

Missing documents are created only when `domain-modeling` resolves a real term or decision.

