# Temporary remote viewing

User explicitly requested a tunnel while away from the computer.

- Native decision origin recovered because prior server was gone: key `1303ae3e`, loopback port `60846`.
- Cloudflare Quick Tunnel: `https://colleague-assumed-careful-draw.trycloudflare.com`.
- Atlas current reviewed image: `/img/0`.
- Anatomy current reviewed image: `/img/1`.
- Both remote PNG responses returned HTTP 200 and SHA-256 matched the exact reviewed local assets.
- Tunnel command uses `--config /dev/null --url http://127.0.0.1:60846 --http-host-header 127.0.0.1:60846 --no-autoupdate`.
- The initial tunnel without the expected Host was stopped after an origin 403; it is not the delivered URL.
- Native decision POSTs reject the external Origin. Deliver direct image links for viewing, not a claim that remote choice submission works.
- A bounded eight-hour local heartbeat keeps this origin available for image viewing; script `/tmp/aohys-review-tunnel-keepalive.py`. No answer is submitted by it. The tunnel depends on the Mac remaining awake and connected.
- No repository directory, private app or production service is served; this is the dedicated design-question server.

The URL and process state are ephemeral; revalidate them on resume.

## Corrected delivery — full HTML and native Impeccable

The user rejected image-only delivery and explicitly requested the full report.
The active delivery is now:

- Report: `https://combined-implied-far-embassy.trycloudflare.com/aohys-diseno-r2.html`.
- Native Impeccable page: `https://combined-implied-far-embassy.trycloudflare.com/`.
- A narrowly scoped proxy listens on loopback `61846`; script `remote-view/server.py`.
- Origin remains the native question server at `60846`, key `1303ae3e`.
- The proxy permits only the report and enumerated Impeccable routes. It validates
  the exact public Host and same-origin POSTs before forwarding to the native
  origin. Native question-key validation is preserved. No general file server.
- Remote HTTP: report/page/PNG routes 200; same-origin heartbeat 204; unrelated
  Origin 403; invalid question key 401; directory and .env paths 404; traversal
  rejected by the edge with 400. Initial check assertions expected only 403/404;
  observed 401/400 are the intended rejection class, not exposure failures.
- The report response preserves the source artifact, with its stale localhost
  comparison link translated to the current tunnel root. Images match reviewed
  files exactly. The artifact label now states the proposal was discarded.
- Browser verification through the public URL: page and report loaded; native
  image expansion worked at width 390. Report image natural width 1536 and
  complete=true; viewport width 390, document scroll width 375. Temporary
  viewport override was reset. No user design answer was submitted by testing.
- Proxy process session `95862`; Cloudflare process session `42424`. Host
  configuration `/tmp/aohys-review-public-host.json`. Bounded local keepalive
  lasts up to 8 hours. Availability also depends on the Mac being awake/online.

This restores delivery and remote interaction plumbing. Both design directions
remain explicitly rejected; it does not constitute a new design proposal.
